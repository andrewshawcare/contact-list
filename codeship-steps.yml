- name: contact_list
  service: contact_list
  command: echo 'Hello, world!'

- type: serial
  steps:
    - name: dockerhub_push
      service: contact_list
      type: push
      image_name: andrewshawcare/contact_list
      registry: https://index.docker.io/v1/
      encrypted_dockercfg_path: dockercfg.encrypted
