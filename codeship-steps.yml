- name: contact_list
  service: contact_list
  command: echo 'Hello, world!'
- name: dockerhub_push
  service: contact_list
  type: push
  image_name: contact_list
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted
